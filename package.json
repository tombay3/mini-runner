{
  "name": "runner1",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "api": "python app.py",
    "dev": "vite --force",
    "build": "vite build",
    "test": "python scripts/sanity_backend.py && node scripts/sanity_frontend.mjs"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
