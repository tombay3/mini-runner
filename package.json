{
  "name": "runner1",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "api": "node scripts/api.mjs",
    "dev": "vite --force",
    "build": "vite build",
    "test": "node scripts/sanity.mjs",
    "evaluate": "node scripts/evaluate-agent.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "vite": "^6.3.5"
  }
}
